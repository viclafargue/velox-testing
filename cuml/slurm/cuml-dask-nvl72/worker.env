#!/bin/bash

# Tweak these per cluster to avoid unnecessary OOM/restarts.
RMM_POOL_SIZE=${RMM_POOL_SIZE:-20GB}
DASK_CUDA_DEVICE_MEMORY_LIMIT=${DASK_CUDA_DEVICE_MEMORY_LIMIT:-0}
DASK_CUDA_MEMORY_LIMIT=${DASK_CUDA_MEMORY_LIMIT:-0}
